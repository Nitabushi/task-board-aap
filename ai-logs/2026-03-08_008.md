# 日付: 2026-03-08

## ユーザーからの指示

フロントエンドの単体テストを実施し、テストコードを格納してください（続き：失敗テストの修正）

## 実施した作業

前回セッションで作成したフロントエンドテストのうち失敗していた7件を修正し、全40テストを通過させた。

### 修正内容

1. **TaskModal.spec.ts** — `submitButton.trigger('click')` では jsdom で `@submit.prevent` が発火しない問題を修正
   - `wrapper.find('form').trigger('submit')` に変更（テスト6, 7）

2. **UserModal.spec.ts** — 同様の form submit 問題を修正
   - `wrapper.find('form').trigger('submit')` に変更（テスト3）

3. **UserTable.spec.ts** — 2点修正
   - `createTestingPinia()` に `{ createSpy: vi.fn }` オプションを追加（Vitest では必須）
   - `vi` を vitest からインポートに追加
   - 削除済みユーザー行のテスト: `deletedRow.text().not.toContain('削除')` が「削除済み次郎」という名前に反応して失敗するため、`findAll('button')` でボタン要素を直接確認する方式に変更

4. **src/__tests__/App.spec.ts** — 削除
   - Vite の初期テンプレートに含まれていた 'You did it!' チェックで、App.vue を RouterView に置き換えた後も残っていた不要なファイル

## 変更したファイル

- `frontend/tests/components/TaskModal.spec.ts` — form.trigger('submit') に修正
- `frontend/tests/components/UserModal.spec.ts` — form.trigger('submit') に修正
- `frontend/tests/components/UserTable.spec.ts` — createSpy・vi インポート追加、ボタン検索ロジック修正
- `frontend/src/__tests__/App.spec.ts` — 削除（不要な自動生成テスト）

## 備考

- 全7テストファイル、40テストがすべてパス
- jsdom 環境では `button[type="submit"].trigger('click')` は form の `@submit.prevent` ハンドラを発火させない。`form.trigger('submit')` を使う必要がある
- `@pinia/testing` の `createTestingPinia` は Vitest では `createSpy: vi.fn` が必須
